---
name: Theming
---

import { Playground, PropsTable } from 'docz'

import { Heading, Text } from '../components/atoms/Typography';
import { EHeadingType } from '../components/atoms/Typography/Heading';
import { ETextType } from '../components/atoms/Typography/Text';
import { createTheme, ThemeProvider } from './theme';

# Theming

Crash Test Components provides a theming solution for colors, typography, and component overrides.

## Colors

TODO

## Typography

The typography theme system can *cascade*, meaning if you set the properties for h1, it will apply to h2, h3, h4, etc... The properties affected by this are `color`, `fontFamily`, and `fontWeight`.

<Playground>
    <ThemeProvider theme={createTheme({
      typography: {
        hero: {
          fontFamily: '"Courier", monospace'
        },
        h2: {
          fontFamily: '"Times New Roman", Times, serif'
        }
      }
    })}>
      <div style={{ padding: 50 }}>
        <Heading type={EHeadingType.HERO}>Hero</Heading>
        <Heading type={EHeadingType.H1}>Heading 1</Heading>
        <Heading type={EHeadingType.H2}>Heading 2</Heading>
        <Heading type={EHeadingType.H3}>Heading 3</Heading>
        <Heading type={EHeadingType.H4}>Heading 4</Heading>
        <Heading type={EHeadingType.H5}>Heading 5</Heading>
        <Heading type={EHeadingType.H6}>Heading 6</Heading>
        <Text type={ETextType.Body}>Body</Text>
        <Text type={ETextType.Caption}>Caption</Text>
        <Text type={ETextType.Label}>Label</Text>
      </div>
    </ThemeProvider>
</Playground>

## Overrides

TODO

## Default Theme

TODO

## Props

<PropsTable of={ThemeProvider}/>